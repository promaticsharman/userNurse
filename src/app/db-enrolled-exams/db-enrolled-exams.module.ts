import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

import { DbEnrolledExamsRoutingModule } from './db-enrolled-exams-routing.module';


@NgModule({
  declarations: [],
  imports: [
    CommonModule,
    DbEnrolledExamsRoutingModule
  ]
})
export class DbEnrolledExamsModule { }
